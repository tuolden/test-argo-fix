apiVersion: apps/v1
kind: Deployment
metadata:
  name: "test_argo_fix"
  namespace: "test_argo_fix"
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: "test_argo_fix"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
